#include <stdio.h>

/**
 * main - prints a phrase with proper grammer
 * Return:0 if no errors encountered
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
